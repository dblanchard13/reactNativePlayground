'use strict';

const ReactNative = require('react-native');

const {
  StyleSheet,
} = ReactNative;

module.exports = StyleSheet.create({
  container: {
    padding: 30,
    marginTop: 65,
    alignItems: 'center'
  },
  description: {
    marginBottom: 20,
    fontSize: 18,
    textAlign: 'center',
    color: '#656565'
  },
});
